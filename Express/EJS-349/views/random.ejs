<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Random Number Generator</title>
</head>

<body>
  <h1>Random Number Generator</h1>
  <p><%= randomNumber %></p>

  <% if (randomNumber % 2 === 0) { %>
  <h4>You generated an even number!</h4>
  <% } else { %>
  <h4>You generated an odd number!</h4>
  <% } %>

  <h4><%= randomNumber%2===0 ? 'EVEN' : 'ODD'%></h4>

</body>

</html>